# Validator (required for every stage handoff)
jsonschema>=4.0

# Confirmatory analysis only (test zone). Not needed to run the generation-zone agents.
pandas>=2.0
lifelines>=0.30

# Note: on systems with setuptools>=60, autograd-gamma (a lifelines dependency) can fail to
# build with "AttributeError: install_layout". Fix: pip install "setuptools<60" first.
